__Create a marketing_campaigns table containing a campaign_id (integer), start_date (date), and budget (decimal)

CREATE TABLE marketing_campaigns(
campaign_id INT,
start_date DATE,
budget DECIMAL (10,2)
);

SELECT * FROM marketing_campaigns