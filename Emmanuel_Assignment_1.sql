__Create a store_locations table containing a store_id (integer), city (varchar), and manager_name (varchar)

CREATE TABLE store_locations(
store_id INT,
city VARCHAR(50),
manager_name VARCHAR (50)
);

SELECT * FROM store_locations;